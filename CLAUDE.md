# Claude Code Pointer

This is **LoogriGram**, a fork. Read `LOOGRIGRAM.md` first — it covers the fork's
state, how to build it, the CI traps that have each already cost a multi-hour
build, and the Android work that has not started yet.

If the premium removal is what you are here for, read
`PREMIUM-HANDOFF.md` too (local, gitignored) — it is mid-job, the work
lives on the `patches` branch, and it records the decisions already taken
so they are not relitigated.

Then read `AGENTS.md`, which is upstream's and remains canonical for code style
and repository-wide conventions. Where the two disagree, `LOOGRIGRAM.md` wins on
anything fork-specific.
